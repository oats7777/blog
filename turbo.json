{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build:wasm": {
      "outputs": ["pkg/**"],
      "cache": true
    },
    "copy:wasm": {
      "dependsOn": ["^build:wasm"],
      "outputs": ["src/lib/wasm/**"],
      "cache": true
    },
    "build": {
      "outputs": ["dist/**"],
      "cache": true
    },
    "dev": {
      "persistent": true,
      "cache": false
    },
    "clean": {
      "cache": false
    },
    "format": {
      "cache": false
    }
  }
}
